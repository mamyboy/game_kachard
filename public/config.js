window.GAME_CONFIG = {
  // ใส่ URL ของ backend ที่รัน Socket.IO เช่น https://your-backend.onrender.com
  // ถ้าว่างไว้: local จะใช้ origin เดิมอัตโนมัติ, แต่บน Vercel จะไม่พยายามเชื่อมต่อเพื่อลด 404
  socketServerUrl: ''
};
